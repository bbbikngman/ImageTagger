<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- API URL Section -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GPT API URL"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"/>

    <EditText
        android:id="@+id/input_gpt_api_url"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter API URL"
        android:inputType="textUri"
        android:layout_marginBottom="16dp"/>

    <!-- API Key Section -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GPT API Key"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"/>

    <EditText
        android:id="@+id/input_gpt_api_key"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter API Key"
        android:inputType="textPassword"
        android:layout_marginBottom="16dp"/>

    <!-- Language Selection Section -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Language Settings"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"/>

    <EditText
        android:id="@+id/input_language_a"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:hint="Language A"
        android:inputType="text"
        android:defaultValue="English"/>

    <EditText
        android:id="@+id/input_language_b"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:hint="Language B"
        android:inputType="text"
        android:defaultValue="Japanese"/>

    <!-- Connection Type Section -->



    <!-- Save Button -->
    <Button
        android:id="@+id/button_save"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Save Settings"
        android:layout_marginTop="16dp"/>

</LinearLayout>